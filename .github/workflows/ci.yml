name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: npm i -D @types/google.maps
    - run: tsc ./public/javascripts/*.ts
    - run: npx tailwindcss -i ./public/stylesheets/tw-src.css -o ./public/stylesheets/tw-dest.css
