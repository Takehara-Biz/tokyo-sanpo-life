<!DOCTYPE html>
<html>

<head>
  <%- include('../../partials/head'); %>
  <script type="module" src="/javascripts/firebaseAuth.js"></script>
</head>

<body>
  <div class="flex flex-col" style="min-height: 100svh; max-height: 100svh;">
    <%- include('../../partials/header'); %>
      <!-- Main Body -->
      <div class="grow overflow-y-scroll flex flex-col">
        <%- include('../../partials/page-title', {title: 'マイページ' }); %>
          <% if (user === undefined) { %>
            <div class="grow flex justify-center items-center">
              <div>
                <p class="text-center">🥳この機能は、<br />ログインしたら使えるようになります〜。</p>
                <p class="text-center">ログインまたは新規登録を行うには、<br />右上のユーザアイコンをクリックしてください。</p>
              </div>
            </div>
            <% } else { %>
              <div class="p-1">
                <h2>あなたの情報</h2>
                <dl>
                  <dt>ID</dt>
                  <dd>
                    <%= user.id %>
                  </dd>
                </dl>
                <dl>
                  <dt>ユーザ名</dt>
                  <dd>
                    <%= user.userName %>
                  </dd>
                </dl>
                <dl>
                  <dt>自己紹介</dt>
                  <dd>
                    <%= user.selfIntroduction %>
                  </dd>
                </dl>
                <dl>
                  <dt>SNS（X）</dt>
                  <dd><a href="<%= user.twitterProfileLink %>" target="_blank">X</a></dd>
                </dl>
                <dl>
                  <dt>SNS（インスタグラム）</dt>
                  <dd><a href="<%= user.instagramProfileLink %>" target="_blank">インスタグラム</a></dd>
                </dl>
                <a class="mt-3 btn bg-accent text-white w-full" href="/user/update" style="background-color:#ff0099;" role="button">
                  <span class="material-symbols-outlined align-[-6px]">
                    manage_accounts
                  </span>
                  編集
                </a>
                <form action="/user/logout" method="post" onsubmit="return confirm('ログアウトしてよろしいですか？');">
                  <button class="mt-3 btn text-white w-full bg-accent" style="background-color:#ff0099;" type="submit">
                    <span class="material-symbols-outlined align-[-6px]">
                      logout
                    </span>
                    ログアウト
                  </button>
                </form>
                <button class="mt-3 btn text-white w-full bg-accent" style="background-color:#ff0099;" onclick="logout();">
                  <span class="material-symbols-outlined align-[-6px]">
                    logout
                  </span>
                  ログアウト2
                </button>
                <button class="mt-3 btn text-white w-full bg-accent" style="background-color:#ff0099;" onclick="return confirm('本当にアカウントを削除してもよろしいですか？');">
                  <span class="material-symbols-outlined align-[-6px]" data-bs-toggle="dropdown" aria-expanded="false">
                    share
                  </span>
                  アカウント削除
                </button>
              </div>
              <% } %>
      </div>
      <%- include('../../partials/footer', {currentMenu: 5}); %>
        <!-- Toast -->
        <%- include('../../partials/toast', {message: "ログインに成功しました"}); %>
        <script>
          window.onload = (() => {
            let toast = false;
            <% if (/* if toast is defined and true */ locals.toast === true) { %>
              toast = true;
            <% } %>
            if (toast) {
              const toastLiveExample = document.getElementById('liveToast')
              const toastBootstrap = bootstrap.Toast.getOrCreateInstance(toastLiveExample);
              toastBootstrap.show();
            }
          });
        </script>
  </div>
</body>

</html>