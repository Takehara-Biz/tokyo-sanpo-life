<!DOCTYPE html>
<html>

<head>
  <%- include('../../partials/head'); %>
</head>

<body>
  <div class="flex flex-col" style="min-height: 100svh; max-height: 100svh;">
    <%- include('../../partials/header'); %>
      <!-- Main Body -->
      <div class="grow overflow-y-scroll">
        <%- include('../../partials/page-title', {title: 'ユーザ新規登録' }); %>
        <div class="p-1 pb-10">
          <div class="accordion mt-3" id="accordionExample">
            <div class="accordion-item">
              <h2 class="accordion-header">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                  data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                  <span class="material-symbols-outlined text-accent">
                    warning
                  </span>
                  <span class="text-accent">登録前の注意事項</span>
                </button>
              </h2>
              <div id="collapseOne" class="accordion-collapse collapse visible" data-bs-parent="#accordionExample">
                <div class="accordion-body">
                  <ul>
                    <li>後からいつでも変更・削除できます。</li>
                    <li>個人を特定できる情報は書かないこと。</li>
                    <li>公序良俗に反する記載は行わないこと。</li>
                    <li>著作権・肖像権に注意すること。</li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
          <form action="/user/create" method="POST" onSubmit="return confirm('本当に登録してもよろしいですか？')">
            <div class="mt-3">
              <label for="userName" class="form-label">ユーザ名（必須）</label>
              <input type="text" id="userName" name="userName" required maxlength="20" class="form-control" />
            </div>
            <div class="mt-3">
              <label for="formFile" class="form-label">アイコン画像（必須）</label>
              <input class="form-control" type="file" id="uploadPhoto" name="iconImage" required
                accept=".jpg, .jpeg, .png" />
            </div>
            <div class="mt-3">
              <label for="selfIntroTextarea" class="form-label">自己紹介</label>
              <textarea name="comment" required class="form-control" id="selfIntro" name="selfIntro" maxlength="200"
                rows="3"></textarea>
            </div>
            <div class="mt-3">
              <label for="formFile" class="form-label">X（ツイッター）のプロフィールURL</label>
              <input type="url" id="xProfileURL" name="xProfileURL" class="form-control" />
            </div>
            <div class="mt-3">
              <label for="formFile" class="form-label">インスタグラムのプロフィールURL</label>
              <input type="url" id="instaProfileURL" name="instaProfileURL" class="form-control" />
            </div>
            <button type="submit" class="mt-3 btn text-white w-full bg-accent" style="background-color:#ff0099;" onsubmit="return confirm('登録してもよろしいですか？');">
              <span class="material-symbols-outlined align-[-6px]">
                person_add
              </span>
              決定
            </button>
          </form>
        </div>
      </div>
      <%- include('../../partials/footer', {currentMenu: null}); %>
  </div>
</body>

</html>