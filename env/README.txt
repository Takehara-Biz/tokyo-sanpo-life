Define the data related to env (prod, stg, dev).
All files must have the same file name and the same properties. (only values can be variable.)

Here the data is copied to under the "public" (Firebase hosting) dir when deploy or before execution on local.